# 🚀 Быстрый старт Neuro-Connector Bot

## Шаг 1: Подготовка

1. Убедитесь, что у вас установлены **Docker** и **Docker Compose**
2. Получите API-ключ от [OpenRouter](https://openrouter.ai/)

## Шаг 2: Настройка

1. Переименуйте `.env.example` в `.env`
2. Откройте `.env` и вставьте ваш `OPENROUTER_API_KEY`

```env
OPENROUTER_API_KEY=ваш_ключ_здесь
```

## Шаг 3: Запуск

Откройте терминал в папке проекта и выполните:

```bash
docker-compose up --build -d
```

## Шаг 4: Тестирование

1. Откройте Telegram
2. Найдите вашего бота
3. Напишите `/start`

## 🎉 Готово!

Бот запущен и готов к работе!

---

### Полезные команды

**Просмотр логов:**
```bash
docker-compose logs bot
```

**Остановка:**
```bash
docker-compose down
```

**Перезапуск:**
```bash
docker-compose restart
```

**Просмотр базы данных:**
```bash
docker exec -it neuro-connector-db psql -U neuro_user -d neuro_connector
```

---

### Что делает бот?

1. **Предприниматель** → Анализирует бизнес-боли и предлагает решения
2. **Стартапер** → Валидирует идеи и дает советы по MVP
3. **Специалист** → Добавляет в базу талантов
4. **Исследователь** → Показывает информацию о компании

🎙️ **Новая функция:** Бот поддерживает голосовые сообщения! Пользователи могут отвечать на вопросы голосом, и бот автоматически распознает речь через AI (Whisper).

Все контакты сохраняются в PostgreSQL для дальнейшей работы.

---

**Поддержка:** Если возникли проблемы, проверьте `README.md` или логи контейнеров.
